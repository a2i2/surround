from surround import Runner
from stages import {project_name}Data


class BatchRunner(Runner):
    def prepare_runner(self):
        # Load data from files or directory here
        self.raw_data = "TODO: Load raw data here"
        self.data = {project_name}Data()
        self.assembler.init_assembler(self.data)

    def prepare_data(self):
        # Parse raw data into pipeline ready format here
        self.data.input_data = self.raw_data

    def run(self, is_training=False):
        # Execute here
        self.prepare_runner()
        self.prepare_data()
        self.assembler.run(None, is_training)

        print("Batch Runner: %s" % self.data.output_data)
