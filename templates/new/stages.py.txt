from typing import Any
from surround import Estimator, SurroundData, Validator, Config


class {safe_project_name}Data(SurroundData):
    input_data: Any = None
    output_data: Any = None


class ValidateData(Validator):
    def validate(self, surround_data: SurroundData, config: Config) -> None:
        if not surround_data.input_data:
            raise ValueError("'input_data' is None")

class Main(Estimator):
    def estimate(self, surround_data: SurroundData, config: Config) -> None:
        surround_data.output_data = surround_data.input_data

    def fit(self, surround_data: SurroundData, config: Config) -> None:
        print("TODO: Train your model here")
